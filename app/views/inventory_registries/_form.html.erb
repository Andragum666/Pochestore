<%= form_with(model: inventory_registry, local: true) do |form| %>
  <% if inventory_registry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inventory_registry.errors.count, "error") %> prohibited this inventory_registry from being saved:</h2>

      <ul>
      <% inventory_registry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :inventory_id %>
    <%= form.text_field :inventory_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
